---
import Layout from '../layouts/Layout.astro';
import { triedPosts } from '../data/tried';
import { tipPosts } from '../data/tips';
import { webSite, organization } from '../utils/jsonLd';

const siteName = 'hrの備忘録';
const siteDesc = '開発と日常の作業メモ。やってみたこと・Tips・ブラウザで動くツールやチートシートをまとめています。';
const jsonLd = [webSite(siteName, siteDesc), organization(siteName)];

const tips = tipPosts.slice(0, 6);

const tools = [
  { name: 'JSON整形', href: '/tools/json-formatter/' },
  { name: 'SQL整形', href: '/tools/sql-formatter/' },
  { name: 'スタイル付きテキスト置換', href: '/tools/styled-text-replacer/' },
  { name: 'X検索', href: '/tools/x-search-builder/' },
  { name: 'リンク化回避', href: '/tools/zero-width-breaker/' },
  { name: 'EXIF編集', href: '/tools/exif-editor/' },
];
const sheets = [
  { name: 'Git', href: '/cheatsheets/git-cheatsheet/' },
  { name: 'Docker', href: '/cheatsheets/docker-cheatsheet/' },
  { name: 'SQL', href: '/cheatsheets/sql-cheatsheet/' },
];
---

<Layout title={siteName} description={siteDesc} jsonLd={jsonLd}>
  <main>
    <header class="masthead">
      <h1>hrの備忘録</h1>
      <p class="tagline">開発と日常の作業メモ</p>
      <p class="lead">
        調べたこと・作ってみたことを、あとで自分が見返すために書き残しています。同じことで詰まった人の手間が少しでも減れば十分です。
      </p>
    </header>

    <section class="feed">
      <div class="section-head">
        <span class="section-label">やってみた</span>
        <a class="section-more" href="/tried/">すべて見る →</a>
      </div>
      <ul class="post-list">
        {triedPosts.map((post) => (
          <li>
            <a class="post-row" href={post.href}>
              <span class="post-main">
                <span class="post-title">{post.title}</span>
                <span class="post-desc">{post.desc}</span>
              </span>
              <time class="post-date">{post.date}</time>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section class="feed">
      <div class="section-head">
        <span class="section-label">Tips</span>
        <a class="section-more" href="/tips/">すべて見る →</a>
      </div>
      <ul class="post-list">
        {tips.map((post) => (
          <li>
            <a class="post-row" href={post.href}>
              <span class="post-main">
                <span class="post-title">{post.title}</span>
                <span class="post-desc">{post.desc}</span>
              </span>
              <time class="post-date">{post.date}</time>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section class="utility">
      <div class="utility-group">
        <div class="section-head">
          <span class="section-label">ツール</span>
          <a class="section-more" href="/tools/">一覧 →</a>
        </div>
        <div class="pill-row">
          {tools.map((t) => (
            <a class="pill" href={t.href}>{t.name}</a>
          ))}
        </div>
      </div>
      <div class="utility-group">
        <div class="section-head">
          <span class="section-label">チートシート</span>
          <a class="section-more" href="/cheatsheets/">一覧 →</a>
        </div>
        <div class="pill-row">
          {sheets.map((s) => (
            <a class="pill" href={s.href}>{s.name}</a>
          ))}
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  main {
    max-width: 720px;
    margin: 0 auto;
    padding: 3.5rem 1.5rem 4rem;
  }

  .masthead {
    border-left: 3px solid var(--color-primary);
    padding-left: 1.15rem;
    margin-bottom: 3.5rem;
  }

  .masthead h1 {
    font-size: 2rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    color: var(--color-text);
    margin: 0;
  }

  .tagline {
    font-family: 'Source Code Pro', ui-monospace, 'SFMono-Regular', monospace;
    font-size: 0.85rem;
    color: var(--color-text-muted);
    margin: 0.4rem 0 0;
  }

  .lead {
    font-size: 1rem;
    line-height: 1.95;
    color: var(--color-text-muted);
    margin: 1.25rem 0 0;
    max-width: 60ch;
  }

  .feed {
    margin-bottom: 3.5rem;
  }

  .section-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    border-bottom: 1px solid var(--color-border);
    padding-bottom: 0.6rem;
    margin-bottom: 0.5rem;
  }

  .section-label {
    font-family: 'Source Code Pro', ui-monospace, 'SFMono-Regular', monospace;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--color-text);
  }

  .section-more {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .section-more:hover {
    color: var(--color-primary);
  }

  .post-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .post-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.4rem 0;
    border-bottom: 1px solid var(--color-border);
    text-decoration: none;
    color: inherit;
  }

  .post-main {
    min-width: 0;
  }

  .post-title {
    display: block;
    font-size: 1.05rem;
    font-weight: 600;
    line-height: 1.7;
    color: var(--color-text);
    transition: color var(--transition-fast);
  }

  .post-row:hover .post-title {
    color: var(--color-primary);
  }

  .post-desc {
    display: block;
    font-size: 0.9rem;
    line-height: 1.8;
    color: var(--color-text-muted);
    margin-top: 0.55rem;
  }

  .post-date {
    flex-shrink: 0;
    font-family: 'Source Code Pro', ui-monospace, 'SFMono-Regular', monospace;
    font-size: 0.8rem;
    color: var(--color-text-muted);
    opacity: 0.85;
    padding-top: 0.3rem;
  }

  .utility {
    margin-top: 3.25rem;
    padding-top: 0.5rem;
    display: grid;
    gap: 1.75rem;
  }

  .pill-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.75rem;
  }

  .pill {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    text-decoration: none;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 0.3rem 0.7rem;
    background: var(--color-bg-card);
    transition: color var(--transition-fast), border-color var(--transition-fast);
  }

  .pill:hover {
    color: var(--color-primary);
    border-color: var(--color-primary);
  }

  @media (min-width: 600px) {
    .utility {
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }
  }
</style>
