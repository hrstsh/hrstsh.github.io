---
import Layout from '../../layouts/Layout.astro';
import { itemList } from '../../utils/jsonLd';

const tips = [
  { name: 'ページのメタ情報を見やすく表示＆コピーするブックマークレット', url: '/tips/meta-info-viewer-bookmarklet', desc: 'title、description、OGタグ、Twitterカードなどを一覧表示し、ワンクリックでコピーできる。' },
  { name: 'X(Twitter)のメディア欄から画像URLを一括取得する方法', url: '/tips/twitter-media-url-extractor', desc: 'メディアタブの画像URLをまとめて取得。開発者コンソールまたはブックマークレットで実行。' },
  { name: 'X(Twitter)のタイムラインを「画像のみ」表示に切り替える', url: '/tips/twitter-image-only-filter', desc: 'テキストツイートを非表示にして、画像・動画だけを表示。ブックマークレットまたはChrome拡張で実現。' },
  { name: 'X(Twitter)のタイムラインで画像・動画付きツイートを非表示にする', url: '/tips/twitter-text-only-filter', desc: '画像・動画付きツイートを非表示にして、テキストツイートだけを表示。ブックマークレットまたはChrome拡張で実現。' },
  { name: 'macOSでファイルパスを一発でターミナルにコピーする', url: '/tips/macos-copy-file-path', desc: 'Option+右クリックやドラッグ&ドロップで簡単コピー。5つの方法を紹介。' },
  { name: 'Macで自作コマンドを作って登録する方法', url: '/tips/macos-custom-command', desc: 'ファイル名一括変換コマンドを例に自作コマンドの作り方と PATH 設定を解説。' },
  { name: 'X(Twitter)の画像をオリジナル画質で保存する方法', url: '/tips/twitter-image-original-quality', desc: 'URLに :orig を付けるだけで高画質保存。ブックマークレット化も。' },
  { name: 'YouTube動画の再生速度を自由に変更する方法（Chrome）', url: '/tips/youtube-playback-speed', desc: 'Chromeのコンソールから3倍速・4倍速の高速再生、0.1倍速の超スロー再生も可能。Premium不要。' },
  { name: 'Chromeのブックマークレットにアイコン（ファビコン）を追加する', url: '/tips/chrome-bookmarklet-favicon', desc: 'javascript: のブックマークはアイコンが出ないので、ファビコンを付けるやり方。' },
  { name: 'X(Twitter)の長文ツイートを自動で非表示にする', url: '/tips/twitter-long-tweet-filter', desc: '長文ツイートだけ隠す Chrome 拡張の作り方（ON/OFF トグル付き）。' },
  { name: 'Mac Terminal（zsh）の表示をカスタマイズする', url: '/tips/mac-terminal-customize', desc: 'ディレクトリ・Gitブランチ・日時・プロンプト表示を ~/.zshrc で整える。' },
];
const jsonLd = itemList('Tips', 'ブラウザや開発で使えるちょっとしたテクニック。ブックマークレット、Chrome拡張、macOSの便利機能などを紹介しています。', 'https://hrstsh.com/tips', tips);
---

<Layout title="Tips - hrの備忘録" description="ブラウザや開発で使えるちょっとしたテクニック。ブックマークレット、Chrome拡張、macOSの便利機能などを紹介しています。" jsonLd={jsonLd}>
  <main>
    <header class="page-header">
      <h1>Tips</h1>
      <p class="subtitle">ブラウザや開発で使えるちょっとしたテクニック</p>
    </header>

    <section class="tips-list">
      <h2>記事一覧</h2>
      <ul class="tip-cards">
        {tips.map((tip) => (
          <li>
            <a href={tip.url} class="tip-card">
              <span class="tip-title">{tip.name}</span>
              <span class="tip-desc">{tip.desc}</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <footer class="page-footer">
      <a href="/">トップに戻る</a>
    </footer>
  </main>
</Layout>

<style>
  main {
    max-width: 860px;
    margin: 0 auto;
    padding: 3rem 1.5rem;
    line-height: 1.7;
  }

  .page-header {
    margin-bottom: 2.5rem;
    padding-bottom: 1.5rem;
    border-bottom: 2px solid transparent;
    border-image: linear-gradient(90deg, var(--color-primary), var(--color-accent)) 1;
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: 1.1rem;
    color: var(--color-text-muted);
  }

  .tips-list h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--color-text);
  }

  .tip-cards {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .tip-card {
    display: block;
    padding: 1.5rem 1.75rem;
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    text-decoration: none;
    color: inherit;
    box-shadow: var(--shadow-sm);
    transition: transform var(--transition-normal), box-shadow var(--transition-normal),
      border-color var(--transition-normal);
  }

  .tip-card:hover {
    transform: translateY(-2px);
    border-color: var(--color-primary);
    box-shadow: var(--shadow-hover);
  }

  .tip-title {
    font-weight: 600;
    color: var(--color-primary);
    display: block;
    margin-bottom: 0.5rem;
    transition: color var(--transition-fast);
  }

  .tip-card:hover .tip-title {
    color: var(--color-accent);
  }

  .tip-desc {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    display: block;
    line-height: 1.6;
  }

  .page-footer {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  .page-footer a {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 500;
    transition: color var(--transition-fast);
  }

  .page-footer a:hover {
    color: var(--color-accent);
  }
</style>
